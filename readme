What is subon?

Subon is a tool for searching active subdomain based on list or single target.


Required Modules :
pip install numpy
pip install argparse
pip install requests


How to install :
git clone https://github.com/RiskEZ/subon
cd subon
python subon.py -h

usage :
-f, --file      Import bulk URL from file
-o, --output    Result output location
-u, --url       Single target check

Any illegal action using this tool is not my responsibilities!
